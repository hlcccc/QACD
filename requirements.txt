# QACD core runtime — deliberately minimal.
# The decision layer (decomposition parsing, feature assembly, BIC-lite
# calibration, MVR statistics, response aggregation, conformal selection) is
# implemented with NumPy only, so the package installs and is testable on a
# CPU-only machine with no model weights.
numpy>=1.23

# --- Optional extras -------------------------------------------------------
# Service mode (qacd.service / `qacd serve`):
#   pip install "fastapi>=0.110" "uvicorn>=0.27" "pydantic>=2.0"
#
# Real OCR instrument channel (qacd.providers.rapidocr):
#   pip install rapidocr-onnxruntime
#
# Real LVLM evidence provider (qacd.providers.llava):
#   pip install "torch>=2.1" "transformers>=4.40" accelerate pillow
#
# Evaluation / research scripts:
#   pip install scikit-learn pandas scipy
#
# Testing:
#   pip install pytest
