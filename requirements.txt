# QACD core runtime — deliberately minimal.
# The decision layer (decomposition parsing, feature assembly, BIC-lite
# calibration, MVR statistics, response aggregation, conformal selection) and
# the evaluation layer are implemented with NumPy only, so the package installs
# and is testable on a CPU-only machine with no model weights.
numpy>=1.23

# --- Optional extras -------------------------------------------------------
# Service mode (qacd.service / `run.py serve`):
#   pip install "fastapi>=0.110" "uvicorn>=0.27" "pydantic>=2.0"
#
# Real OCR instrument channel (qacd.providers.RapidOCRProvider):
#   pip install rapidocr-onnxruntime
#
# Real LVLM evidence provider (qacd.providers.LLaVAProvider):
#   pip install "torch>=2.1" "transformers>=4.40" accelerate pillow
#
# Reproducing the frozen results from the evidence bundle: NumPy only, no extra.
#
# Testing:
#   pip install pytest                              # 114 of the 128 tests, no extras
#   pip install pytest "fastapi>=0.110" "pydantic>=2.0" httpx
#                                                   # all 128, incl. the HTTP service layer
#                                                   # (httpx is required by fastapi's TestClient)
